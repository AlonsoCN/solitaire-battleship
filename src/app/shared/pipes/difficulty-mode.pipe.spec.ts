import { DifficultyModePipe } from './difficulty-mode.pipe';

describe('DifficultyModePipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultyModePipe();
    expect(pipe).toBeTruthy();
  });
});
