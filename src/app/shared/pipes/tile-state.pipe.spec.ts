import { TileStatePipe } from './tile-state.pipe';

describe('TileStatePipe', () => {
  it('create an instance', () => {
    const pipe = new TileStatePipe();
    expect(pipe).toBeTruthy();
  });
});
